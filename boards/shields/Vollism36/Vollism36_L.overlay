#include "Vollism36.dtsi"
#include <input/processors.dtsi>
#include <dt-bindings/zmk/input_transform.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/input/input-event-codes.h>
#include <input/processors/sensor_rotation.dtsi>
// #include <input/processors/zip.dtsi> // ← この行が必須

#define QWE 0
#define WIN 1
#define CAD 2
#define LWR 3
#define RSE 4
#define ADJ 5
#define MOV 6
#define NUM 7
#define BLE 8

&kscan0 {
        col-gpios
            = <&xiao_d 10 GPIO_ACTIVE_HIGH>
            , <&xiao_d 9 GPIO_ACTIVE_HIGH> 
            , <&xiao_d 8 GPIO_ACTIVE_HIGH> 
            , <&xiao_d 7 GPIO_ACTIVE_HIGH>
            ;
};

/ {
    trackball_listener {
        compatible = "zmk,input-listener";
        device = <&trackball>;
        input-processors = <&sensor_rotation>;
        // automouse {
        //         layers = <WIN>;
        //         input-processors = <&zip_temp_layer MOV 100>; // Move layer for 600ms after activity
        //         process-next;
        // };
        scroll {
                layers = <RSE>;
                input-processors = <&zip_xy_transform (INPUT_TRANSFORM_Y_INVERT)>
                                 , <&zip_xy_scaler 1 30>
                                 , <&zip_xy_to_scroll_mapper>;
        };
        // 👈 【追加】NUMレイヤーでの方向キー変換を明示的に定義
        // arrows_mapper {
        //         layers = <NUM>;
        //         input-processors = <&zip_xy_to_arrow_mapper>; // トラックボールの動きを方向キーに変換
        //     // zip_xy_to_arrow_mapper のパラメーターを調整したい場合は以下のように記述
        //     // input-processors = <&zip_xy_to_arrow_mapper 10>; // 10は感度/閾値 (任意)
        // };
    };
};

/ {
    input_processors {
        sensor_rotation: sensor_rotation {
            compatible = "zmk,input-processor-sensor-rotation";
            #input-processor-cells = <0>;
            rotation-angle = <340>;
        };
    };
};

&pinctrl {
    spi0_default: spi0_default {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 0, 5)>,
                <NRF_PSEL(SPIM_MOSI, 0, 4)>,
                <NRF_PSEL(SPIM_MISO, 0, 4)>;
        };
    };

    spi0_sleep: spi0_sleep {
        group1 {
            psels = <NRF_PSEL(SPIM_SCK, 0, 5)>,
                <NRF_PSEL(SPIM_MOSI, 0, 4)>,
                <NRF_PSEL(SPIM_MISO, 0, 4)>;
            low-power-enable;
        };
    };
};

&spi0 {
    status = "okay";
    compatible = "nordic,nrf-spim";
    pinctrl-0 = <&spi0_default>;
    pinctrl-1 = <&spi0_sleep>;
    pinctrl-names = "default", "sleep";
    cs-gpios = <&gpio0 9 GPIO_ACTIVE_LOW>;

    trackball: trackball@0 {
        status = "okay";
        compatible = "pixart,pmw3610";
        reg = <0>;
        evt-type = <INPUT_EV_REL>; // あるいは <INPUT_EV_ABS>
        spi-max-frequency = <2000000>;
        irq-gpios = <&xiao_d 0 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>;
        cpi = <600>;
        evt-type = <INPUT_EV_REL>;
        x-input-code = <INPUT_REL_X>;
        y-input-code = <INPUT_REL_Y>;
        arrows {
                layers = <NUM>;
                bindings =
                    <&kp RIGHT_ARROW>,
                    <&kp LEFT_ARROW>,
                    <&kp UP_ARROW>,
                    <&kp DOWN_ARROW>;

                tick = <10>;
                wait-ms = <0>;
                tap-ms = <10>;
        };
    };
};